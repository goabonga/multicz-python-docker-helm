# OWASP ZAP rule overrides for the owasp-web job — passed to
# zap-baseline.py via `-c rules.tsv`. Api-only overrides live in
# .zap-rules.tsv at the repo root (passed to zap-api-scan.py in
# owasp-api).
#
# Format: <rule-id>\t<IGNORE|WARN|FAIL>\t<comment>
# Tabs only — ZAP's parser rejects spaces.
#
# 10109 — Modern Web Application.
# Informational notice: ZAP detected an SPA pattern (client-side
# routing, no traditional links). Not a vulnerability, just a
# scan-strategy hint for human reviewers.
10109	IGNORE	informational SPA detection — not a vulnerability
