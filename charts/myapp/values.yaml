# Umbrella values: each subchart's values are namespaced under its
# chart name. Override anything from charts/myapp-{api,web}/values.yaml
# here.

myapp-api:
  replicaCount: 1
  service:
    port: 8000

myapp-web:
  replicaCount: 1
  service:
    port: 80
